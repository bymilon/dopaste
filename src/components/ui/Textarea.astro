---
type Props = {
    id: string;
    name: string;
    value?: string;
    placeholder?: string;
    disabled?: boolean;
    autofocus?: boolean;
    invalid?: boolean;
    rows?: number;
    ariaDescribedby?: string;
    class?: string;
};

const {
    id,
    name,
    value = "",
    placeholder = "",
    disabled = false,
    autofocus = false,
    invalid = false,
    rows = 16,
    ariaDescribedby,
    class: className = "",
} = Astro.props;
---

<div class:list={["ui-control-shell", className]}>
    <textarea
        id={id}
        name={name}
        rows={rows}
        placeholder={placeholder}
        disabled={disabled}
        autofocus={autofocus}
        aria-describedby={ariaDescribedby}
        aria-invalid={invalid ? "true" : "false"}
        class="ui-control ui-data ui-textarea flex-1 resize-none px-3 py-2 sm:px-4 sm:py-3"
    >{value}</textarea>
</div>
